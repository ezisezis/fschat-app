const configuration = {
  socket: {
    host: "localhost",
    port: "8080",
    autoConnect: false,
    reconnection: false,
    timeout: 10000
  },
  maximumMessageLength: 1000
};

export default configuration;
